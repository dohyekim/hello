fam_names = list("김이박최강고윤엄한배성백전황서천방지마피")


sung = random.choice(fam_names)          # 랜덤으로 fam_names(list)에서 하나씩 뽑아옴

random.random() vs random.randrange()  cf. random.randint()

random.uniform(a, b) # a와 b사이의 랜덤 실수

random.sample(lst, k=3) # sampling할 때 k에 지정해준 수 만큼 가져온다. 불특정 n개를 가져오고 싶을 때


